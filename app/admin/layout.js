export const metadata = {
  title: 'The Kiley Record — Editor',
  robots: { index: false, follow: false, noarchive: true }
};

export default function AdminLayout({ children }) {
  return children;
}
